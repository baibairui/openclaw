# GUI 兜底内部参考

适用时机：

- 没有 API
- 也没有可用或稳定的浏览器路径
- 目标存在于桌面软件、系统界面或纯视觉环境

原则：

- GUI 是补空白能力
- 不作为默认主路径
- 每次 GUI 操作前都先确认前两层不可行或明显不适合
- 使用 GUI 时优先追求少步骤、可见证据和可恢复性

当前接线：

- 默认内置通用 macOS GUI 执行器
- 通过 `scripts/gui-run.mjs` 统一进入
